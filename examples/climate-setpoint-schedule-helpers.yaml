# Helpers for Nodalia Climate Card weekly setpoint schedules.
# Guide: docs/climate-setpoint-schedule.md
# The card stores JSON in this entity (via webhook) so schedules persist without saving the dashboard.

input_text:
  climate_schedule_dormitorios:
    name: Nodalia consignas — Dormitorios
    max: 255
    initial: '{"enabled":true,"slots":[]}'

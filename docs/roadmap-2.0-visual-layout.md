# Visual layout editor → 2.0.0

> [!NOTE]
> Archived design plan. The preserved WYSIWYG branch is not part of stable `2.0.0`; reconsider it in a future feature cycle instead of treating this file as the active project roadmap.

The WYSIWYG visual layout editor for the light card (and related `nodalia-visual-layout-editor.js` work) is **not** part of stable **`1.2.0`**.

## Branch

All commits through **`1.2.0-alpha.20`** (editor, live preview, grid guides, palette thumbnails, etc.) are preserved on:

**`future/2.0.0-visual-layout`**

Use that branch if this experimental editor is resumed in a future development cycle.

## Historical stable line (`1.2.0`)

Stable **`1.2.0`** shipped the climate setpoint schedule, Scenes Card, Power Flow home popup, and related **`1.2.0-alpha.*`** work. Stable `2.0.0` also leaves the visual layout editor on the preserved branch above rather than merging it into the production bundle.
